import * as React from "react"
import * as ReactDom from "react-dom"

const App = () => {
	return <div> A FlutureCode web production </div>
}

ReactDom.render(<App />, document.getElementById("root"))
